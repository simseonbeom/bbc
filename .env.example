# 게시된 스프레드시트 기본 URL (?output=csv 앞부분까지)
# 파일 → 공유 → 웹에 게시 → 전체 문서 → CSV → 게시 후 URL에서 ?output=csv 앞까지만 복사
VITE_SHEET_BASE=

# 각 탭의 GID (편집 모드에서 탭 클릭 시 URL 끝 #gid=XXXXX 숫자)
# 기본 첫 번째 탭은 0
VITE_GID_MEMBERS=0
VITE_GID_NOTICES=
VITE_GID_ACTIVITIES=
VITE_GID_PHOTOS=
